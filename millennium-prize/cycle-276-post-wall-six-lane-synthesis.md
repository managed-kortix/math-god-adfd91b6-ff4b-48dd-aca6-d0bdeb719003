# Cycle 276: unconditional synthesis after the closed C275 intake

## Terminal input and non-repetition rule

Accept `C275-WALL` unconditionally. The sole packet
`P275-AMT4-EXISTENCE` stopped at admission item 2: neither outcome of its
fixed four-input census has an explicit implication to a quantified part of
official P versus NP. The later certificate and resource defects do not need
repair for this decision.

The intake is closed. Do not resubmit the same packet, supply its missing
implication after the fact, run the four-input census, replace it by a nearby
finite MCSP instance, or open another packet-admission cycle. All route stops
through Cycle 275 remain in force. In particular, do not search for the missing
Navier datum, refine the Hodge cover, enlarge an RH cutoff, cycle through BSD
curves, repair local additive charging, or return to a two-dimensional
Yang--Mills contraction norm.

## Unconditional six-lane synthesis

Use the established lexicographic score

\[
 L=(\text{barrier crossing},\text{non-equivalence},
    \text{finite falsifiability},\text{official transfer}).
\]

| lane | score | post-C275 disposition |
|---|---|---|
| Navier--Stokes | `(1,1,0,1)` | The critical-`L^3` factor-two target remains a separated sufficient condition for the periodic official existence alternative. `ND251` is preserved, but every admitted production family and the no-datum certificate-capacity continuation are stopped. |
| Hodge | `(1,1,0,0)` | `DG-ALPHA-WALL` remains terminal for the frozen model. The fixed-degree target is separated, but no common-model PEL representative packet or finite support mechanism is available. |
| RH | `(1,0,0,1)` | The complete logarithmically tapered Nyman--Beurling endpoint estimate still transfers to RH, but its surviving cancellation statement is RH-strength and has no separated finite rejection gate. |
| BSD | `(0,1,1,0)` | Exact isolated arithmetic theorems remain bounded and curve-specific. No uniform primary-control or determinant-line bridge to the official leading-term formula is frozen. |
| P versus NP | `(0,1,1,0)` | `C275-WALL` confirms that a finite `AMT(4,s,2)` fact does not cross the asymptotic unrestricted-model barrier. Local additive charging remains refuted, and no relational MMW transfer is supplied. |
| Yang--Mills | `(0,1,0,0)` | The finite-lattice gap remains off the continuum trajectory. The two-dimensional tail map is an isometry or expansion in the audited norms, and no four-dimensional scale-uniform normal contraction is available. |

Navier--Stokes remains lexicographically first. It cannot be assigned as an
active proof campaign because it has no stop-rule-compliant finite production
object. Promoting a lower lane would discard the established ordering without
a new checkpoint. Reopening packet admission would repeat the loop just closed.
The next main is therefore one deliberate focused-discovery action in the
leading lane.

The other five lanes remain bounded scouts in monitor-only status for this
cycle. They may record an independently proved lemma or obstruction, but they
receive no search, enumeration, cutoff, nearby-instance, or missing-input
assignment.

## Selected discovery: `C276-NS-EXCURSION-BUDGET`

For a smooth mean-zero periodic Navier--Stokes solution, write

\[
 X(t)=\|u(t)\|_{L^3(\mathbb T^3)}^3,
\]

and, using the standard smooth approximation at zeros of `u`, define

\[
 \mathcal P(u)=\int_{\mathbb T^3}p\,u\mathbin\cdot\nabla|u|,
 \qquad
 \mathcal D(u)=-\int_{\mathbb T^3}|u|u\mathbin\cdot\Delta u\geq0.
\]

The exact critical-norm identity is

\[
 X(t)-X(0)=3\int_0^t\bigl(\mathcal P(u(s))-
 \nu\mathcal D(u(s))\bigr)\,ds.                    \tag{276.1}
\]

Freeze the following structural conjecture, rather than a datum-production
request.

**Critical first-excursion budget conjecture.** For every such solution and
every `tau` before its maximal smooth time for which
`X(t) <= X(tau)` on `[0,tau]`,

\[
 3\int_0^\tau\bigl(\mathcal P(u(s))-\nu\mathcal D(u(s))\bigr)\,ds
 < \frac78 X(\tau).                                \tag{CEB}
\]

The constant is fixed, not optimizable. Combining `(276.1)` and `(CEB)` gives
`X(tau)<8X(0)`, hence `||u(tau)||_3<2||u(0)||_3` at every new maximum. The
standard endpoint `L^infinity_t L^3_x` continuation criterion then gives the
full unforced periodic smooth-existence alternative in the official
Navier--Stokes statement. Thus `(CEB)` has an explicit official implication;
it is not claimed proved, and it may be stronger than regularity.

This formulation isolates the only source of critical-norm growth--integrated
pressure work after viscous payment--and is not a new Fourier datum, packet,
trajectory screen, or certificate-capacity repair.

## One bounded first experiment

Run exactly one analytic mechanism audit, `C276-CEB-ONE-SPLIT`, with no
trajectory generation.

1. Starting only from `(276.1)`, the pressure equation
   `-Delta p = partial_i partial_j(u_i u_j)`, incompressibility, and periodic
   integration by parts, split the spacetime domain once at the frozen level
   `|u|=X(tau)^(1/3)`. No second amplitude level, parameter optimization, or
   frequency decomposition is allowed.
2. On the low-amplitude region use the exact maximum normalization. On the
   high-amplitude region, attempt to charge the pressure term to
   `nu*D(u)` plus at most `7X(tau)/24` after time integration. Every use of a
   Calderon--Zygmund, Holder, interpolation, or truncation estimate must retain
   its explicit constant and direction.
3. Terminate after the first complete inequality chain or the first
   unchargeable term. Return exactly one of:
   `CEB-SPLIT-PASS`, an exact derivation of `(CEB)`;
   `CEB-SPLIT-NOGO`, a scaling family or exact sign configuration proving that
   every estimate allowed by this one-split mechanism misses `7/8`; or
   `CEB-SPLIT-WALL`, the first term requiring an unavailable scale-sensitive
   estimate. A wall names the missing estimate but does not launch a search for
   it.

The experiment is bounded to one split and one written inequality chain. It
may not use the C265/P3 payload, finite Fourier or Galerkin data, floating
optimization, a nearby constant, or a second mechanism. `PASS` would prove the
official periodic alternative after hostile verification; `NOGO` or `WALL`
retires only this pressure-budget mechanism and returns the portfolio to a new
written synthesis.

Record:

`C275 WALL ACCEPTED AND INTAKE CLOSED; NO RE-ADMISSION LOOP; SIX LANES SYNTHESIZED; ALL RECORDED ROUTE STOPS PRESERVED; NEXT MAIN C276-NS-EXCURSION-BUDGET FOCUSED DISCOVERY; ONE-SPLIT ANALYTIC EXPERIMENT ONLY; NO DATUM OR TRAJECTORY SEARCH; NO MILLENNIUM CLAIM.`
