# Cycle 119: portfolio rotation to the Yang--Mills annular gate

The Hodge Hermitian route was retired after producing an explicit finite-field
representative but no noncircular relative-Chow lift.  The new main funnel is a
Yang--Mills temporal-annulus contraction test, with its equivalence boundary
stated explicitly.

For a temporal annulus of fixed physical width `r0`, let `K_(a,L)` be the full
gauge-invariant conditional-expectation channel from its inner to outer
boundary, after quotienting every exactly transmitted vacuum, center-flux, and
superselection label.  The binary target is either

\[
 \|K_{a,L}f\|_2^2\le q\|f\|_2^2,
 \qquad q<1,
\]

for every centered physical boundary function with one `q` uniform in cutoff,
volume, boundary condition, and sector, or an explicit sequence whose ratios
tend to one.  A valid positive result must also identify this temporal channel,
after Gauss projection and the OS null quotient, with a complete core for the
vacuum-complement transfer operator.  Without that bridge it proves screening,
not a mass gap.  With full physical-algebra completeness it is essentially the
previous physical-slab contraction criterion, not a free new implication.

## Exact calibration and hostile model

For the central `SU(2)` Wilson convolution channel, Peter--Weyl diagonalization
gives

\[
 s_j(\beta)=\frac{I_{2j+1}(\beta)}{I_1(\beta)}.
\]

After the center quotient, integer spins remain and an `N`-layer bridge has

\[
 \rho_{\max}=\left(\frac{I_3(\beta)}{I_1(\beta)}\right)^N.
\]

The singular values decrease with spin, so high representation labels do not
escape in this diagonalizable model.  This is calibration only: the full
four-dimensional Wilson slab generates the complete loop/polymer/intertwiner
algebra and is not diagonalized by one-link characters.

Fixed-observable SDPI is decisively insufficient.  The exact four-state channel

\[
 K_N=B_{1/2}\otimes B_{1-1/N}
\]

has uniform contraction on the visible first binary coordinate but full
centered norm `1-1/N -> 1`.  It models a cutoff-dependent hidden physical
sector escaping every fixed observable algebra.

## Other scouts

- **Navier--Stokes:** a bare first-passage dissipation tax is false.  A two-wave
  packet transfers critical shell energy on nonlinear time while its dissipated
  fraction tends to zero at high Reynolds number.  Only a completed rapid
  `K -> 2K` no-return passage remains a meaningful checkpoint.
- **P versus NP:** native `MCSP[(log N)^2]` is sparse and a fixed-power
  superlinear lower bound has full magnification reach, but every short labeled
  sample is cheaply fitted.  No junk-robust global consistency invariant was
  produced.
- **RH:** functional symmetry and zero repulsion do not create descendants of
  an off-line zero; symmetric finite-order functions can contain an isolated
  off-line quartet.  A branching-zero theorem needs genuinely arithmetic
  production.
- **BSD:** modular symbols at every prime still differentiate character
  directions, not the complex Mellin variable.  A rank-two archimedean
  Gross--Zagier kernel would be genuine production, but none is constructed.
- **Hodge:** the nonlinear relative-`CH^2/W_2` realizability of the explicit
  projected Hermitian-plane cycle remains a bounded scout only.

No Yang--Mills construction or Millennium result is claimed.
