# Bounded scout checkpoints — cycle 35

These are bounded exact or independently verified checkpoints for the five
non-main Millennium routes.  They do not alter the active RH funnel and do not
resolve any Millennium problem.

## Birch--Swinnerton-Dyer

The curve `389.a1` is independently recorded with algebraic rank bounds
`[2,2]`, generators `(0,0),(1,0)`, semistability, and maximal mod-`5` image.
This certifies the rank-two and irreducibility calibration independently of an
analytic-rank computation.  Exact cyclotomic coefficients remain missing.

## Hodge conjecture

The Fermat cubic plane-incidence locus has projective dimension

\[
9+(55-10)=54.
\]

Its normal-map cokernel is the line spanned by `uvw`, so the `uvw` coefficient
is the unique first-order transverse obstruction.  This remains a local
divisor checkpoint only.

## Navier--Stokes

For the cyclic shear triad

\[
u=(\sin z,1.3\sin x,0.7\sin y),
\]

the exact pressure-frequency contribution to the initial critical-`L^3`
derivative vanishes by coordinate parity.  This symmetric calibration cannot
produce growth; a less symmetric interacting triad is required.

## P versus NP

Among all `2^15=32768` labeled six-vertex graphs, exactly `27554` are
3-colorable and `5214` are not.  The colorable family uses all `122` distinct
labeled three-coloring masks.  This fixes the finite universe for the queued
hitting-set computation but proves no circuit lower bound.

## Yang--Mills mass gap

For the rounded-time lower bound,

\[
{\log(1/q)\over r_0+a}
< {\log(1/q)\over a\lceil r_0/a\rceil}
\le {\log(1/q)\over r_0}.
\]

Hence its continuum limit is exactly `log(1/q)/r_0`.  Ceiling normalization is
not the missing mass-gap step; convergence on the full vacuum complement is.
