# Bounded scout checkpoints — cycle 36

These are five bounded exact checkpoints or obstructions for the non-main
Millennium routes.  They do not alter the active RH funnel and do not resolve
any Millennium problem.

## Birch--Swinnerton-Dyer

`T^2+5` has Iwasawa invariants `[lambda,mu]=[2,0]` in `Z_5[[T]]` but is not
divisible by `T`.  The invariants recorded for `389a1` therefore cannot replace
exact certification of its constant and linear cyclotomic moments.

## Hodge conjecture

For a plane `P` in a smooth cubic fourfold, `P^2=3`, `P.h^2=1`, and `h^4=3`.
The incidence divisor consequently carries the integral primitive vector
`3[P]-h^2` of square `24`, fixing the precise lattice calibration but not other
Hodge components.

## Navier--Stokes

For every cyclic shear `u=(a sin z,b sin x,c sin y)`, the pressure source
`sum_(i,j) partial_i u_j partial_j u_i` vanishes identically.  This whole ansatz
has constant pressure and cannot exhibit pressure-driven initial `L^3` growth.

## P versus NP

The `122` six-vertex coloring masks have minimum exact cover size `90`: all
proper three-block partitions are necessary and sufficient.  This closes
mask deletion for the finite witness-DNF test but has no unrestricted-circuit
consequence.

## Yang--Mills mass gap

The contractions `T_n=qI+(1-q)P_(e_n)` converge on every fixed quadratic form
to `qI` and eventually contract every fixed coordinate trial space, yet each
has an escaping eigenvector of eigenvalue one.  Fixed-correlator convergence
cannot substitute for uniform contraction on the full vacuum complement.
