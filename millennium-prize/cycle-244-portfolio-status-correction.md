# Cycle 244: portfolio correction without `KI240 PASS`

## Mathematical status correction

Retract the recorded status `KI240 PASS`. Cycle 241 gives a finite connective
Karoubi normal form: every retract in the declared graph-generated thick
category has a finite twisted-complex model. That result only reduces `KI240`
to the Cycle 200 finite-twisted-complex claim.

Cycle 200 does not complete the required last step. Its acyclic ordering shows
that cross-vertex twisting data are upper triangular, but the text does not
prove that a nonzero vertex obstruction on the associated graded survives as a
nonzero class in the total `Ext^2` complex. In particular, it supplies neither
a filtered-complex survival argument nor a chain-level functional annihilating
all total coboundaries. The assertion that every relevant diagonal term would
have to leave and return to a vertex is not by itself that missing proof.
The exact obstruction to that inference is
`F_i[0] -> F_j[2] -> F_i[4]`: the cross-`Ext^3` arrows have a nonzero product
in self `Ext^6`, of total degree two. Refined cell/shift/bar filtrations do not
force that product to vanish; without computed higher differentials they do
not prove the diagonal class is permanent.

Therefore the corrected statuses are:

- `Theorem 241.A`: retained as the abstract finite Karoubi-normal-form result;
- Cycle 200 finite twisted-complex obstruction: `INCOMPLETE` at obstruction
  survival;
- `KI240`: `INCOMPLETE`, not `PASS`, because its final implication depends on
  the incomplete Cycle 200 step;
- no closure of the graph-generated Hodge category is claimed.

A future `KI240 PASS` must repair the Cycle 200 step by an exact filtered
survival theorem or by an explicit cocycle/dual-cocycle computation valid for
every finite twisted complex. Repeating strictification, projector splitting,
or bounded packet searches does not repair this dependency.

## Corrected portfolio review

Do not award any portfolio credit for the retracted pass. Also do not promote
the two new scouts:

- `NB242 PASS / CALIBRATION` is the small `N=3` inequality only. It has zero
  barrier-crossing and zero official-transfer value and gives no control of a
  cofinal dyadic tail.
- `F242` remains `INCOMPLETE` before the explicit `phi`, difference scheme,
  differential, and curve class are supplied. Its symmetry reduction and rank
  filters do not constitute a candidate or an exhaustive rejection.

For currently actionable next targets, the corrected comparison remains

| lane | score | corrected status |
|---|---|---|
| Navier--Stokes | `(1,1,0,1)` | The factor-two critical-`L^3` route retains official transfer, but still lacks an admitted finite production family. |
| Hodge | `(1,1,0,0)` | `KI240` is incomplete and cannot be counted as a closure; `F242` is only a structurally reduced non-graph scout with no instantiated computation. |
| RH | `(1,0,0,1)` | The cofinal Nyman--Beurling estimate remains RH-strength; `NB242` is only calibration and earns no score increment. |
| BSD | `(0,1,1,0)` | The fixed curve/field all-primary packet remains retired by `HK236 WALL`. |
| P versus NP | `(0,1,1,0)` | The exact OBDD results still do not cross the unrestricted-circuit barrier. |
| Yang--Mills | `(0,1,0,0)` | Finite-lattice coercivity remains off the continuum trajectory. |

## Active phase

Persist the Cycle 242 rotation to focused Navier--Stokes discovery `LA242`, but
change its justification. The rotation no longer follows from a terminal
`KI240 PASS`; it follows from the corrected comparison of actionable targets
and from the absence of a presently valid Hodge proof campaign. `LA242` is
still discovery, not an assigned proof campaign: admission requires one
deterministic smooth Lagrangian family, a signed velocity-`L^3` amplification
greater than two, complete generated-scale bounds, finite exact breakers, and
the Cycle 211 full-PDE transfer data.

Do not reactivate graph enumeration merely because `KI240` is incomplete. Its
only legitimate Hodge continuation is a direct repair of the Cycle 200
obstruction-survival gap. Keep `F242` as an inactive bounded scout until its
missing explicit geometric data are printed, and keep `NB242` as calibration.

No Millennium problem is claimed solved.
