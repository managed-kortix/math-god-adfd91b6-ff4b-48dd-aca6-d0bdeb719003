# Cycle 270: strategic rescore after the frozen H269 cover wall

## Terminal input

`H269-DG-DIAG-ALPHA` has returned the prescribed terminal output
`DG-ALPHA-WALL`. The declared two-open Weierstrass cover is affine and the big
diagonal is a regular codimension-three immersion, but none of the four
pair-chart diagonal ideals is principal. Hence the frozen scalar
Cech--Koszul model cannot be formed. The stop is exact and complete.

Do not refine the cover automatically. Replacing the cover, trivializing the
diagonal ideal on smaller opens, or silently switching to a line-bundle Koszul
resolution would be a new architecture rather than a repair inside Cycle 269.

## Hodge re-entry test

The global line-bundle Koszul resolution is mathematically legitimate. For the
three factor diagonals, the canonical sections of the three pulled-back line
bundles `O(Delta_j)` cut out the big diagonal regularly, so their Koszul complex
avoids the false demand for scalar generators on the frozen charts. This
observation repairs only the presentation wall.

It does not supply the chain data that motivated `H269-DG-DIAG-ALPHA`. A usable
next packet would still have to freeze all of the following in one common
model:

1. finite local trivializations or finite projective-module presentations for
   the three diagonal line bundles and all overlap maps;
2. Cech representatives for `h_i`, `n_i`, and `alpha=h_1 wedge h_2`;
3. the nine first-order PEL deformations over `Q(i)[epsilon]/(epsilon^2)`;
4. chain-level Atiyah and Kodaira--Spencer maps, including the induced
   variation of `alpha`;
5. an exact finite cohomology quotient or dual pairing deciding each resulting
   obstruction class.

The retained dossier contains none of items 2--4 in the proposed line-bundle
model. Merely naming the canonical Koszul resolution therefore recreates the
H268 transfer wall in a different presentation. It has no finite disposition
gate until those representatives are supplied independently. No
`H270-LB-KOSZUL` campaign is authorized.

## Six-lane rescore

Use the established lexicographic score

\[
 L=(\text{barrier crossing},\text{non-equivalence},
     \text{finite falsifiability},\text{official transfer}).
\]

| lane | score | Cycle 270 frontier |
|---|---|---|
| Navier--Stokes | `(1,1,0,1)` | `ND251` remains a sufficient, officially transferring target. Static equal-energy endpoints and the transfer interface are retained. A self-induced smooth Euler orbit with directed velocity-`L^3` ratio above two is still missing. No finite production family is currently admitted. |
| Hodge | `(1,1,0,0)` | The line-bundle Koszul complex repairs nonprincipality abstractly but does not provide the missing PEL Cech representatives or variation-of-arrow map. The full chain calculation is therefore not presently bounded. |
| RH | `(1,0,0,1)` | The exact dyadic decrement remains an RH-strength signed divisor-sum inequality. Cycle 269 rejects grouped-cell positivity; moving to larger cutoffs or a quadratic-sign search would retry the stopped architecture without a transfer-separating lemma. |
| BSD | `(0,1,1,0)` | The `43a1` packet is closed, exact, curve-specific, and routine. No family-level determinant-line bridge is available. |
| P versus NP | `(0,1,1,0)` | Additive local gate charging is exactly refuted, including fractional omniscient charging. No frozen shared-core correction transfers to unrestricted circuit lower bounds. |
| Yang--Mills | `(0,1,0,0)` | The retained finite-lattice strong-coupling gap remains off the continuum weak-coupling trajectory, and the frozen weighted RG contraction is refuted. |

After removal of Hodge's packet-local finite-falsifiability credit,
Navier--Stokes is lexicographically first. Select `ND251` as the main funnel,
but distinguish the exact mathematical target from candidate discovery: no
numerical or symbolic production campaign is authorized until one new family
passes the bounded admission gate below.

Record:

`H269 DG-ALPHA-WALL ACCEPTED; DO NOT RETRY OR REFINE THE COVER; LINE-BUNDLE KOSZUL NOT ADMITTED WITHOUT PEL CHAIN REPRESENTATIVES; RESUME ND251 AS MAIN FUNNEL AT ARCHITECTURE ADMISSION ONLY; NO MILLENNIUM CLAIM.`

## Exact target `ND251`

Use the genuine-three-dimensional realization adopted in Cycle 265. Produce one
deterministically specified smooth mean-zero divergence-free velocity `v_0` on
`T^3`, one time `T>0`, and one exact margin `eta>0` such that its unique smooth
Euler solution on the certified interval satisfies, in one declared direction,

\[
 \|v(T)\|_{L^3(\mathbb T^3)}
 >(2+\eta)\|v(0)\|_{L^3(\mathbb T^3)}.
\tag{ND251}
\]

The packet must then supply the retained Cycle 211 fixed-time stability,
quantitative inviscid convergence, amplitude scaling, and directed endpoint
enclosures needed for the official Navier--Stokes transfer. A Galerkin or
floating-point crossing is candidate evidence only.

## Next bounded frontier `ND270-ADMISSION`

The next action is not an open-ended Euler search. It is a fail-closed admission
test for one proposed production architecture. Before any trajectory
computation, the proposal must print:

1. one finite symbolic genuinely three-dimensional family of smooth real
   periodic divergence-free data; finite initial Fourier support is allowed
   provided item 4 controls every mode generated by the full Euler evolution;
2. one fixed compact parameter box and one fixed finite set of times or a
   symbolic time interval, with no adaptive horizon choice;
3. an exact nonstationarity and nonplanarity certificate and the Cycle 271
   finite local-production certificate: at one printed rational parameter and
   one direction `sigma in {+1,-1}`, a directed lower bound
   `sigma mathcal P_3(u_*)>=q>0` and upper bound
   `||u_*||_3^3<=M`, including pressure and tail enclosures; this certifies
   active change of the complete velocity `L^3`, not factor-two capability;
4. a closed analytic-norm majorant for every generated scale on the declared
   interval, together with explicit first-failure inequalities;
5. a frozen outward-rounded promotion rule requiring a directed complete-field
   ratio strictly above `2+eta_0` for a predeclared rational `eta_0>0`;
6. a finite resource manifest and the exact interface from a promoted member
   to the retained full-Euler and inviscid-transfer certificates.

Return exactly one admission output:

1. **`ND270-ADMIT`:** every item is present and internally consistent; freeze
   the family, box, times, thresholds, resources, and certificate interfaces in
   a separate compute manifest before running anything;
2. **`ND270-ARCH-WALL`:** print the first missing or false admission item and
   stop that architecture without search, tuning, or substitution;
3. **`ND270-DUPLICATE`:** give an exact reduction to a retired finite-support,
   planar skew-product, generalized-Beltrami, or previously frozen numerical
   architecture and stop.

For a perturbation of a retired Kida--Pelz/tangent profile, "infinite Fourier
support" alone cannot prevent the third outcome. Apply the Cycle 272 mechanism
deletion test: unless a signed, indispensable core--tail or tail--tail lemma has
a strict favorable margin that disappears when the tail is deleted, with all
adverse interactions and generated scales enclosed on the fixed interval, the
proposal is `ND270-DUPLICATE`. Missing proof of that essentiality is the first
`ND270-ARCH-WALL`. See
`navier-stokes/cycle-272-kida-tail-duplicate-audit.md`.

`ND270-ADMISSION` is a bounded review gate, not a claim that a qualifying family
exists. Cycle 272 subsequently supplies the P3 architecture and corrects the
initial-support condition: see
`navier-stokes/cycle-272-p3-finite-support-admission-audit.md`. That datum passes
items 1--4 plus novelty, but no computation is pending until items 5 and 6 are
frozen in a separate manifest.

The phrase "mechanism capable" is not an additional plausibility test. Cycle
271 proves that factor-two capability is a trajectory property and reserves it
for the frozen promotion rule in item 5. Admission item 3 tests only exact local
complete-`L^3` production; see
`navier-stokes/cycle-271-nd270-admission-threshold-audit.md`.

## Stop rules

- Do not retry `H269-DG-DIAG-ALPHA` with another cover, a cover refinement,
  local principalizations, Dolbeault representatives, Fourier--Mukai methods,
  or a line-bundle Koszul complex. Hodge re-entry requires a new written packet
  that already supplies the missing PEL chain representatives and a finite
  terminal gate.
- Do not construct the complete seven-graph dg enhancement, return to the H268
  two-cell census, add packets, or escalate cell count.
- Do not rerun, enlarge, or tune `C264-DE1`, `C266-3DDE1`, `C267-KP1`, the
  Cycle 255 family, finite-Fourier families, or planar triangular skew-products.
  The resource stop is not permission to lower its preflight.
- Do not run a broad random, evolutionary, or parameter search under
  `ND270-ADMISSION`. The gate evaluates one finitely printed architecture; it
  does not generate one.
- Do not accept a trajectory confined to fixed finite Fourier support,
  generalized Beltrami stationarity, passive-scalar growth, vorticity growth,
  or projected/Galerkin `L^3` growth as an `ND251` mechanism. Finite support at
  the initial time is allowed when the full generated tail is enclosed.
- On `ND270-ARCH-WALL` or `ND270-DUPLICATE`, retire only that proposed
  architecture. Do not substitute a nearby family automatically. On
  `ND270-ADMIT`, freeze a new compute manifest and apply its own first-failure
  stop rules.
- Do not restart the RH dyadic cutoff search, seek larger finite positivity
  ranges, or infer total endpoint monotonicity from quadratic or cellwise signs.
  A new RH campaign requires a non-equivalent global sign mechanism and a fresh
  strategic review.
- No numerical screen, bounded scout, admission pass, local obstruction, or
  special-case theorem is a Hodge, Navier--Stokes, RH, or other Millennium
  result.
