# Cycle 264: formal closure of the bounded `43a1` side packet

## Evidence received

This review occurs after both terminal decisions required for the bounded side
campaign. Cycle 263 proves

\[
 \Sha(43\mathrm a1/\mathbf Q)=0
\]

within the stated theorem-backed PARI/eclib trust boundary. The exact packet
identifies the normalized Heegner point, proves the Mordell--Weil index is one,
closes every residual image, applies the cited all-odd-prime Kolyvagin bound,
and closes the 2-primary part by full 2-descent. The separate publication-gate
audit classifies the result as `ROUTINE/KNOWN-IN-PRACTICE`: it is a standard
low-conductor specialization of classical Gross--Zagier--Kolyvagin plus
2-descent and has no defensible publication novelty.

These decisions are compatible. The first is an internal curve-specific
theorem; the second denies novelty and every external workflow action. Neither
is a full BSD leading-term theorem, a family-rank theorem, an official BSD
transfer, or a Millennium result.

## Formal packet closure

The single side packet authorized in Cycle 261 is **closed successful at its
internal theorem target** and **closed no-go at its publication gate**. Preserve
all proof artifacts and audits as an internal reproducibility case study. Do
not reopen the packet to improve packaging, seek a more favorable novelty
description, run the optional Magma replay merely to support publication, or
cycle through nearby curves, isogenous labels, or quadratic fields.

No publication workflow action is authorized or taken. In particular, create
no publication-manifest row, manuscript or preprint submission, journal or
author contact, OCB submission or resolution report, X announcement, or other
external outreach. The retained `sha-43a1/` paper is an internal
reproducibility artifact only and is not an announcement package.

Record:

`43a1 SIDE PACKET CLOSED: INTERNAL CURVE-SPECIFIC THEOREM; ROUTINE/KNOWN-IN-PRACTICE; NO NOVELTY; NO PUBLICATION, CONTACT, OCB, X, OR OTHER EXTERNAL ACTION.`

## Main-funnel decision

Keep Navier--Stokes `ND251` as the main funnel. Cycle 263 validates the
energy-and-enstrophy-preserving midpoint Galerkin mechanism required after the
Cycle 258 retirement, but only in finite dimension. The concurrently frozen
`C264-DE1` screen uses that mechanism for a three-member directed-endpoint
family with predeclared residual and promotion gates; its pre-compute freeze is
an admissible main-lane numerical campaign, not an outcome. Neither the
integrator validation nor the freeze supplies a deterministically certified
Euler datum with amplification strictly above `2+eta` or a full-Euler tail
enclosure. Thus they preserve the exact target without earning a theorem claim.

The six-lane scores remain

| lane | score | Cycle 264 status |
|---|---|---|
| Navier--Stokes | `(1,1,0,1)` | `ND251` remains a separated sufficient target with official transfer. The invariant-preserving finite Galerkin integrator and frozen `C264-DE1` directed screen are available, but no screen outcome, certified crossing, or full-Euler enclosure is yet recorded. |
| Hodge | `(1,1,0,0)` | `F242` and the fixed theta-section architecture are retired. `KI240` remains incomplete at chain-level obstruction survival. |
| RH | `(1,0,0,1)` | The surviving explicit Nyman--Beurling endpoint estimate remains RH-strength and lacks a separated finite rejection gate. |
| BSD | `(0,1,1,0)` | The `43a1` theorem is exact and bounded but routine, curve-specific, and non-transferring. Its closure gives no family-level barrier credit. |
| P versus NP | `(0,1,1,0)` | Exact all-order OBDD results remain outside unrestricted circuits and the relational MMW resource transfer. |
| Yang--Mills | `(0,1,0,0)` | The finite-lattice strong-coupling gap remains off the continuum trajectory; no scale-uniform cutoff-removal packet is available. |

Record:

`ND251 REMAINS MAIN; C264-DE1 IS A FROZEN NUMERICAL SCREEN ONLY; NO NAVIER--STOKES OR MILLENNIUM CLAIM.`

## Next five bounded scouts

The non-main lanes receive one bounded scout each. These are admission-sized
tests, not proof campaigns, and none inherits authorization from the closed
`43a1` packet.

| lane | bounded scout | exact output and stop rule |
|---|---|---|
| Hodge | `H264-KI-MIN` | Freeze the minimal two-vertex shifted-return model realizing `F_i[0] -> F_j[2] -> F_i[4]`. Compute the total endomorphism complex and either print a chain-level cocycle/dual-cocycle proving survival of the diagonal `Ext^2` class or an explicit differential killing it. Stop after this model; a pass is only a mechanism test and is not `KI240 PASS`, while a kill retires this repair ansatz. |
| RH | `R264-NB-DYADIC` | Freeze one complete dyadic endpoint functional for the actual logarithmically tapered Mobius coefficients, including constant, linear, retained-interval, and oscillatory terms. Produce an exact signed divisor-sum inequality with a symbolic scale parameter or an exact counterexample at the first admissible scale. Stop on either output; finite positivity alone receives no RH credit and no cutoff escalation is allowed. |
| P versus NP | `P264-AMT-GADGET` | Freeze one selector-free MCSP address/data gadget for the `AMT(n,s,m)` template. Prove a one-gate easy/hard circuit-size separation after maximizing over every unqueried component, or print an explicit shared circuit violating the claimed direct sum. Stop with that gadget; even a pass is an MCSP transfer checkpoint, not `P != NP`. |
| Yang--Mills | `Y264-RG-TAIL` | Freeze one reflection-positive blocking step for square-lattice `SU(2)` and write its full character/loop tail. Prove a representation-cutoff-uniform weighted contraction bound on the induced complement or exhibit a tail family violating every declared weight budget. Stop after one blocking architecture; no finite truncation or fixed-coupling gap counts as continuum progress. |
| BSD | `B264-DL-AUDIT` | Perform one source-bounded determinant-line applicability audit, with no new curve or field computation: test whether a named published rank-one modular-elliptic-curve theorem yields the exact complex leading-term identity on an explicitly stated nontrivial class without assuming BSD or its missing equality. Return a source-backed implication or an equivalence/hypothesis wall, then stop. Do not reopen `43a1`, substitute another isolated curve, or initiate publication activity. |

The scouts may preserve reusable exact lemmas or exact obstructions, but they do
not alter the lane ordering unless a later written strategic review records a
proved checkpoint or decisive falsification. No public claim or publication
workflow follows from their assignment.
