# Cycle 274: conditional rescore for the ND251 certificate-capacity result

## Status and ordering rule

Cycle 273 closes the frozen P3 endpoint manifest before trajectory work.  The
analytic cap proves that its ratio cannot reach `201/100` anywhere on the
declared slab.  This retires that datum/horizon manifest, not `ND251` and not
the full-Euler certificate interfaces retained from Cycles 211, 255, 256, and
265.

The pending certificate-capacity analysis must return exactly one of
`WITNESS`, `NOGO`, or `WALL`.  This memo pre-registers the strategic treatment
of all three outputs.  It does not assign a branch before the capacity result
exists.  In particular, no lane rotation, replacement Euler family, numerical
screen, or next-main declaration is authorized by this conditional memo.

Apply the following order:

1. freeze and verify the capacity artifact;
2. classify it as exactly one of the three outputs below;
3. synthesize that output with the six current dossiers and stop rules;
4. only then name the next main funnel and its first bounded action in a new
   unconditional strategic record.

## Common score

Use the established lexicographic score

\[
 L=(\text{barrier crossing},\text{non-equivalence},
    \text{finite falsifiability},\text{official transfer}).
\]

The capacity result can change only the current actionability of the Navier--
Stokes certificate architecture.  It cannot manufacture an endpoint crossing,
alter the official-transfer theorem, or increase another lane's score.  The
five comparison rows therefore remain fixed until result synthesis:

| lane | provisional score | retained frontier |
|---|---|---|
| Hodge | `(1,1,0,0)` | `DG-ALPHA-WALL` is accepted.  Re-entry requires a finite packet already containing the missing common-model PEL chain representatives; no cover refinement or automatic line-bundle campaign is allowed. |
| RH | `(1,0,0,1)` | The dyadic Nyman--Beurling sign problem remains RH-strength and has no separated finite rejection gate.  Larger cutoffs and renewed cell-sign searches are forbidden. |
| BSD | `(0,1,1,0)` | Exact isolated arithmetic packets remain non-transferring.  No family-level determinant-line bridge is frozen. |
| P versus NP | `(0,1,1,0)` | Local additive charging is refuted and restricted OBDD results still lack an unrestricted-circuit transfer.  No shared-core correction is frozen. |
| Yang--Mills | `(0,1,0,0)` | Finite-lattice gaps remain off the continuum trajectory, and the frozen contraction mechanisms are closed. |

## Branch A: `WITNESS`

Assign `WITNESS` only if the artifact gives one finite, replayable allocation of
all retained certificate obligations within a declared resource envelope.  It
must bind the full-Euler head, generated tail, time discretization, endpoint
cubature, strict outward ratio comparison, and inviscid-transfer fields without
assuming the desired crossing.  A complexity estimate, successful Galerkin
run, omitted tail, or unverified resource forecast is not a witness.

Conditional Navier--Stokes row:

| lane | conditional score | consequence for synthesis |
|---|---|---|
| Navier--Stokes | `(1,1,1,1)` | Certificate capacity is finite and falsifiable for one frozen architecture.  The witness earns actionability credit only; it is not an `ND251` crossing and does not revive the rejected P3 datum/horizon. |

At synthesis, compare this row with the five fixed rows.  If it remains first,
the unconditional record may continue Navier--Stokes only through the exact
finite architecture bound by the witness.  It may not substitute a nearby
datum, tune the horizon, or call capacity feasibility a PDE result.

## Branch B: `NOGO`

Assign `NOGO` only if the artifact proves that every allocation in the declared
certificate model violates an explicit necessary inequality or finite resource
bound.  The quantifiers must cover the whole frozen model, not merely the P3
manifest or one implementation.  State exactly which retained interfaces the
theorem covers.  A failed run, timeout, memory exhaustion, coarse estimate, or
absence of a found allocation is not a no-go.

Conditional Navier--Stokes row:

| lane | conditional score | consequence for synthesis |
|---|---|---|
| Navier--Stokes | `(1,1,0,1)` | Retire only the certificate architecture within the proved quantifiers.  `ND251` remains a sufficient officially transferring target, but no bounded production/certification campaign is thereby available. |

At synthesis, remove every actionability claim actually covered by the no-go,
then compare the resulting Navier row with the five fixed rows.  Do not weaken
the ratio, lower preflight bounds, enlarge a Fourier box, or relabel the same
architecture.  A new Navier main would require a non-equivalent written
architecture with its own finite gate; otherwise synthesis must consider the
other dossiers or focused portfolio discovery.

## Branch C: `WALL`

Assign `WALL` only when the bounded analysis identifies the first missing
theorem, datum, or decidable interface needed to prove either feasibility or
infeasibility.  Name that item precisely and explain why the retained artifacts
do not determine it.  `WALL` is epistemic closure of this capacity analysis: it
is neither evidence of adequate capacity nor a universal no-go.

Conditional Navier--Stokes row:

| lane | conditional score | consequence for synthesis |
|---|---|---|
| Navier--Stokes | `(1,1,0,1)` | Preserve `ND251`, pause the blocked capacity architecture, and give it no finite-falsifiability credit unless a separate bounded packet already resolves the named missing item. |

At synthesis, compare this row with the five fixed rows without converting the
missing lemma into a proposed campaign.  Re-entry through that route requires a
new packet that supplies the missing item and a finite terminal gate.  No
resource retry or nearby-family substitution follows from `WALL`.

## Result-synthesis gate

The unconditional synthesis must quote the verified output and its scope,
dispose of the covered architecture, print all six final scores, resolve ties
by the score and the existing stop rules rather than plausibility, and name one
main funnel with one exact bounded next action.  Until that record exists, the
state is:

`CAPACITY RESULT PENDING; THREE CONDITIONAL RESCORES PREPARED; ND251 PRESERVED BUT PAUSED; NO ROTATION AND NO NEXT MAIN SELECTED.`

No capacity witness, no-go, wall, bounded scout, or strategic rescore is a
Navier--Stokes or other Millennium result.
