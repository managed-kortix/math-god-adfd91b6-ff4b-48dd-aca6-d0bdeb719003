# Cycle 251: fail-closed correction and rotation to Navier dynamics

## Superseding decision

This note supersedes the earlier Cycle 251 assignment of the bounded Hodge
`F251` campaign. The concurrent tangent-intersection theorem proves that every
map in the `F242` support architecture fails the source-level G0
closed-immersion gate: a map is either non-unramified or has a nonzero source
collision. Therefore there is no `F242` candidate to send to G2 or G3, and the
proposed `5^54` enumeration must not be run.

Record `F242 RETIRED: G0 NO-GO`. This is an exact retirement of that support
architecture only. It is not a Hodge-conjecture result, does not complete
`KI240`, and does not rule out unrelated Hodge supports.

After removing the superseded Hodge score increment, rotate the main funnel to
focused Navier--Stokes dynamics `ND251`. This is a strategic selection of the
next bottleneck, not an admission that the required Euler orbit exists and not
a Navier--Stokes result.

## Six-lane hostile review

Use the established lexicographic score

\[
 L=(\text{barrier crossing},\text{non-equivalence},
     \text{finite falsifiability},\text{official transfer}).
\]

Scores describe the next justified target. They do not credit a retired route
or turn a discovery objective into a finite proof campaign.

| lane | score | exact frontier after the F242 no-go |
|---|---|---|
| Navier--Stokes | `(1,1,0,1)` | Cycle 251 supplies an exact Hamiltonian equal-energy rearrangement with a strict velocity-`L^3` gap, but not a self-induced Euler path. The factor-two implication and Cycle 211 transfer interface remain official-facing. No finite dynamical production family is yet admitted. |
| Hodge | `(1,1,0,0)` | `F242` is retired at G0. `KI240` remains incomplete at obstruction survival; weight and leading punctured-support repairs are walled. Re-entry requires an exact chain-level repair or a genuinely new non-graph support, not another F242 or graph-packet search. |
| RH | `(1,0,0,1)` | The fixed-dyadic contraction route is terminal. The surviving `O(1/log N)` Nyman--Beurling estimate is RH-strength and has no separated finite rejection gate. |
| BSD | `(0,1,1,0)` | `HK236 WALL` retires the fixed curve/field all-primary packet. Isolated primary calculations do not restore uniform barrier crossing. |
| P versus NP | `(0,1,1,0)` | Exact OBDD and anti-sharing results remain restricted-model results and do not cross the unrestricted-circuit or relational MMW transfer barrier. |
| Yang--Mills | `(0,1,0,0)` | The finite-lattice strong-coupling gap is retained, but `CA235 FAIL` leaves it off the cutoff-removal trajectory; no scale-uniform continuum packet is available. |

Navier--Stokes is lexicographically first. Its zero finite-falsifiability
coordinate is retained deliberately: the present static theorem does not
supply a finite exact test for self-induced Euler accessibility.

## Exact target `ND251`

Let `K=\nabla^\perp\Delta^{-1}` on mean-zero functions on `T^2`. Produce one
deterministically specified smooth mean-zero vorticity `omega_0`, one time
`T>0`, and one symbolic margin `eta>0` such that the unique smooth solution of

\[
 \partial_t\omega+(K\omega)\mathbin\cdot\nabla\omega=0,
 \qquad \omega(0)=\omega_0,
\]

satisfies, in one declared direction,

\[
 \|K\omega(T)\|_{L^3(\mathbb T^2)}
 >(2+\eta)\|K\omega(0)\|_{L^3(\mathbb T^2)}.             \tag{ND251}
\]

Time reversal may be used to orient a certified factor-two decay in the form
`(ND251)`. Equimeasurability and homogeneous `H^-1` equality then follow from
the Euler evolution; they are not substitutes for proving that evolution.

An admissible `ND251` packet must contain all of the following:

1. a finite symbolic description of `omega_0`, its parameter box, and the
   signed Biot--Savart interaction intended to create the strict gap;
2. a closed estimate for the complete self-induced Euler solution on `[0,T]`,
   including self-interaction, reverse transfer, periodic images, smoothing
   errors, and every generated scale, with the margin `eta` left positive;
3. exact or outward-rounded certificates for every indispensable sign and
   remainder inequality, together with explicit first-failure outputs for the
   declared family;
4. the Cycle 211 transfer data: fixed-time Euler stability, quantitative
   inviscid convergence, amplitude scaling for each fixed positive viscosity,
   and directed endpoint `L^3` enclosures preserving a ratio strictly above
   two.

Items 1--3 certify only the Euler breaker of the universal factor-two lemma.
Item 4 is additionally required before claiming transfer to the periodic
Navier--Stokes existence alternative.

## Fail-closed rules

- The Cycle 251 Hamiltonian rearrangement is kinematic input only. Its
  externally generated Hamiltonian isotopy must not be called an Euler path.
- Failure of one finite `ND251` family retires only that family. Sampling,
  floating trajectory agreement, or a Galerkin crossing is not a rejection or
  a pass.
- Do not search for another static rearrangement unless it supplies new data
  that constrain the self-induced equation. Static equal-energy and factor-two
  separation already exist.
- Do not reactivate `F242`, run its enumeration, send any F242 map to G2/G3, or
  reinterpret its G0 no-go as evidence for the Hodge conjecture.
- Do not credit `KI240` without an exact obstruction-survival proof valid for
  every finite twisted complex, and do not substitute another equivalent
  graph-support presentation.

No Hodge, Navier--Stokes, or other Millennium problem is claimed solved.
