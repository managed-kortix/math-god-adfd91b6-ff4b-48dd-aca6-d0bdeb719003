# Cycle 237: strategic rotation after `HK236 WALL`

## Terminal checkpoint decision

Assign `HK236 WALL` under the frozen Cycle 236 rule. The checkpoint does not
pass any of its four conjunctive items:

1. The Heegner hypothesis and auxiliary rank-zero nonvanishing are exact, and
   the analytic index-square factor is exactly `64`, but this does not determine
   the integral Mordell--Weil index without the unknown `Sha` factor. No exact algebraic
   CM-trace evaluation or directed elliptic-logarithm isolation identifies the
   integral trace with either rational candidate `+/-8P`.
2. Applicable integral Kolyvagin bounds are prime-by-prime. Cha's theorem does
   not cover `5,23,1499`, and the original Kolyvagin theorem requires residual
   surjectivity certificates not currently supplied at those primes. Hence no
   audited all-prime integer `C_A` is available.
3. The global lower-height calculation depends on an undirected
   Cremona--Siksek computation, while the upper bound for the Heegner trace
   depends on the uncertified trace identity. Hence no unconditional integer
   cutoff `M` or `B_A` exists in the required certificate.
4. Without items 2 and 3, the checkpoint cannot print the required complete
   finite-prime list under the stated divisibility.

The exact modular-symbol and arithmetic results are preserved. This outcome is
`WALL`, rather than `FAIL`, because the audits prove unavailability of the
required effective integral factor and directed height budget; they do not
contradict the underlying Heegner datum. Item 1 also fails closed, but that
earlier coordinate obstruction does not change the terminal classification
forced independently by items 2 and 3.

Do not replace item 2 by the weaker support implication

`p | #Sha(A/Q) => p | I_A or p in {5,23,1499}`,

do not treat the conditional cutoff `M=35` as certified, and do not move to a
nearby Heegner field. The frozen curve/field architecture is retired exactly as
specified by Cycle 236.

## Six-lane rotation review

Use the established lexicographic score

\[
 L=(\text{barrier crossing},\text{non-equivalence},
    \text{finite falsifiability},\text{official transfer}).
\]

| lane | score | Cycle 237 status |
|---|---|---|
| Navier--Stokes | `(1,1,0,1)` | The universal factor-two `L^3` lemma remains a separated sufficient statement with official endpoint-continuation transfer. All admitted finite-support mechanisms are retired, and no genuinely infinite-tail candidate is frozen, so finite falsifiability is not currently earned. |
| Hodge | `(1,1,0,0)` | The fixed-degree relative-Chow target remains separated, but degree-one and degree-two carriers failed consecutively. The stop rule forbids degree escalation or incidence enumeration without a new support mechanism. |
| RH | `(1,0,0,1)` | Nyman--Beurling and canonical endpoint limits retain official transfer, but the surviving production estimate is RH-strength and has no separated finite rejection gate. |
| BSD | `(0,1,1,0)` | Isolated Selmer and Kurihara computations remain exact, but `HK236 WALL` shows that the promoted all-primary packet does not deliver its effective integer divisibility or height cutoff. More primary checks, a weakened support checkpoint, or nearby-field cycling do not cross the barrier. |
| P versus NP | `(0,1,1,0)` | All-order OBDD hardness has exact finite witnesses but does not cross the unrestricted-circuit or relational MMW resource-transfer barrier. |
| Yang--Mills | `(0,1,0,0)` | The explicit finite-lattice theorem remains archived off the continuum trajectory. Re-entry still requires a non-equivalent scale-uniform continuum mechanism with a finite falsifier. |

Navier--Stokes leads lexicographically, but its missing finite production object
prevents assignment as an active proof lane. The correct next main funnel is
focused discovery, not a weakened BSD continuation and not a numerical Navier
campaign.

## Focused discovery gate

Select the **Navier genuinely-infinite-tail critical-amplification admission
gate**. A proposal is admitted as the next main gate only if it supplies all of
the following on paper.

1. Freeze one deterministic family of smooth real divergence-free initial data
   and a signed nonlinear transfer architecture whose Euler evolution generates
   an infinite Fourier tail. Fixed Fourier support, a finite sequence of exact
   closures, disconnected packet replication, and unstructured random starts
   are inadmissible.
2. Prove a scale-uniform interaction estimate controlling reverse transfer,
   packet collisions, and the entire generated tail while giving an endpoint
   lower bound `||u(T)||_3>(2+eta)||u(0)||_3` for explicit `T` and `eta>0`.
   Galerkin growth and a margin produced only by scaling or aliasing do not
   satisfy this item.
3. State an exact finite rejection condition for every declared constant and
   budget. The condition must be capable of returning a failed signed identity,
   a nonpositive margin, or a directed tail inequality violation; failed
   floating search is not a terminal result.
4. Give the residual norm and explicit inequalities needed by the existing
   full-PDE enclosure interface, including inviscid-limit/amplitude transfer to
   fixed positive viscosity and directed endpoint `L^3` cubature. The margin
   must survive all tails, viscosity, time discretization, and cubature errors.

Passing these four admission items freezes one finite certification campaign
and promotes Navier--Stokes to the assigned main gate. An exact obstruction
retires only the declared infinite-tail architecture and returns to focused
portfolio discovery. Until then, do not enlarge Galerkin boxes, revive the
Cycle 224--225 packets, optimize a weaker amplification threshold, or resume
isolated BSD primary computations.

No Millennium problem is claimed solved.
