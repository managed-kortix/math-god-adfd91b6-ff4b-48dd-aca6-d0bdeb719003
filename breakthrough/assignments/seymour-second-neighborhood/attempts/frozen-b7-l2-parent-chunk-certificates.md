# Frozen B7-l2 parent-chunk certificates

## Scope

This package is restricted to the four clean Frozen Seymour `B7-l2`
root-cardinality profiles and the exact cap-50 chunk cover committed at
`721249ac550ad173333643d4658e23325af56187`. The 652 leaves form 163 ordered,
disjoint, exhaustive chunks in each profile and compose to exactly 8,119
parents (32,476 profile/parent incidences). No other residual group or broader
Seymour claim is included.

## Checked certificates

Pinned CaDiCaL 1.7.3 produced one textual LRAT for every scout-UNSAT leaf, and
pinned `lrat-check` accepted all 652 before staging. The retained `xz -3`
artifacts total 4,409,362,076 bytes. The package ledger uses consecutive greedy
ranges under an exclusive 90,000,000-byte cap and binds each range by a hash of
its ordered artifact identities.

The strict certificate ledger binds the exact committed cover manifest, CNF
hash ledger, 20-second scout, producers, independent structural checker,
closure verifier, finalizer, and the complete transitive local Python runtime.
The verifier authenticates every compressed artifact and freshly decompresses
every proof to check its raw LRAT identity. Representative replay regenerates,
structurally audits, and checks the first and last chunk of each of the four
profiles with the pinned independent checker.

The composition verifier independently reconstructs the cover and requires all
652 certificate rows to close all four profiles and exactly 8,119 parents.
Staging JSON is disposable only after the moved corpus, ledgers, strict identity
pass, composition pass, and representative replay all succeed.
