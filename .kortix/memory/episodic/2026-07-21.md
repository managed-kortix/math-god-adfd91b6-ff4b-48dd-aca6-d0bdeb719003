# 2026-07-21

## X infrastructure test

- Re-ran `bash scripts/setup-harness.sh`; Python/SymPy, PARI/GP, Lean bootstrap,
  and `x-cli` completed successfully. The harness wrote all five injected
  `TWITTER_*` values into the corresponding `X_*` configuration fields. No
  credential values were printed or persisted in the repository.
- Identity test against `GET https://api.x.com/2/users/me` with OAuth 1.0a:
  HTTP 401 `Unauthorized`; no handle was returned.
- Independent legacy identity test against
  `GET https://api.x.com/1.1/account/verify_credentials.json`: HTTP 401,
  code 89, `Invalid or expired token.`
- Independent bearer-token read test against API v2: HTTP 401 `Unauthorized`.
- Required real write test,
  `x-cli tweet post "test post from the lab, ignore"`: HTTP 401
  `Unauthorized`; therefore no tweet was created and no tweet ID exists to
  delete. Delete test was necessarily not executable.
- Diagnosis: this is not evidence of a read-only app permission (which would
  normally authenticate and reject the write with 403). The injected
  consumer/access-token credential set is invalid, expired, revoked, or
  internally mismatched. Regenerate/reinject a mutually matching OAuth 1.0a
  key/token set; also enable Read and Write when regenerating the user access
  token.

## Research orientation

- Loaded the memory OS and surveyed fresh arXiv math.CO candidates.
- Leading candidate: Akbari--Kumar--Mohar--Pragada--Zhang, arXiv:2506.07264,
  Conjecture 1.2: every connected n-vertex graph with m >= n+1 has positive
  adjacency square energy s^+(G) >= n. It is certificate-shaped and the paper
  reports exhaustive verification only through n=9; selection and attack-plan
  persistence follow after the priority infrastructure interruption.
